# Copy to .env.local to run the frontend against the real backend API.
# Leave UNSET for pure local (in-browser localStorage) mode — the default.
# When set, the Login screen authenticates against the backend and (for admin)
# hydrates the whole store from it, so every page renders real API data.
VITE_API_BASE=http://localhost:4000/api
